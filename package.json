{
  "name": "starvia-split",
  "version": "0.1.0",
  "private": true,
  "description": "STARVIA static astrology app",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "check:js": "node --check app.js && node --check astrology-core.js && node --check data/thai-astrology-content.js && node --check js/reading-helpers.js && node --check astro-renderers.js && node --check ui-actions.js"
  },
  "devDependencies": {
    "jsdom": "^24.1.3",
    "vite": "^5.4.21",
    "vitest": "^2.1.9"
  }
}
